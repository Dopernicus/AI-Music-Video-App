pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // Add other repos here later if needed
        // maven { url "https://jitpack.io" }
    }
}

rootProject.name = "AI-Music-Video-App"
include ':app'
